VITE_COORDINATOR_URL=http://localhost:3001

# Auth dashboard — doit correspondre à DASHBOARD_TOKEN dans .env (racine du projet)
# Laisser vide = mode dev sans authentification
# VITE_DASHBOARD_TOKEN=
