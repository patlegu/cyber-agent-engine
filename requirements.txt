# =============================================================================
# LORA FACTORY - Standalone Component Dependencies
# Versions épinglées depuis l'environnement de référence (Python 3.11, fév 2026)
# =============================================================================

# --- Core & Data ---
pydantic==2.12.5
pyyaml==6.0.3
aiofiles==25.1.0
httpx==0.28.1
python-dotenv==1.2.1
jinja2==3.1.6
numpy==2.2.6

# --- LLM Inférence & Training ---
openai==2.21.0
anthropic==0.81.0
google-generativeai==0.8.6
torch==2.9.1
transformers==4.57.6
accelerate==1.12.0
bitsandbytes==0.49.2
peft==0.18.1
trl==0.24.0

# --- Utilities ---
rich==14.3.2
click==8.3.1
tenacity==9.1.4
python-multipart==0.0.22
fastapi==0.129.0
uvicorn==0.41.0

# --- Optional (if specific trainers used) ---
# unsloth  # Requires specific install command: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# --- Infrastructure ---
vllm==0.15.0
psutil==7.2.2
datasets==4.3.0

# --- Anonymisation ---
# Dev local : pip install -e /srv/anonyfiles
# Prod      : décommenter la ligne ci-dessous
# anonyfiles_core @ git+https://github.com/patlegu/anonyfiles.git@main
spacy>=3.7.0

# --- Development & Testing ---
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov>=4.1.0
